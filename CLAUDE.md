# CLAUDE.md — ALLFOLIO 에이전트 금지 목록

에이전트 파이프라인(개발·검증 노드)이 이 레포에서 지켜야 할 금지 규칙.

> **SSOT는 노션이다.** 원본: [\[에이전트\] 그래프 엔지니어링 — 금지 목록 v1](https://app.notion.com/p/3d3db7e1aa3b816089f8f27a6c0002c7) (AF-162).
> 이 파일은 노션 원본의 복사본이다. 규칙을 바꿀 때는 **노션을 먼저 고치고** 이 파일에 반영한다.
> 위반 사례가 나오면 노션 하단에 기록 → 규칙 문구 보강 → 이 파일 갱신.

## 공통 원칙

금지 항목에 부딪히면 **우회하지 말고 즉시 중단** → 태스크에 사유 기록 → 사람에게 에스컬레이션.
"금지라서 다른 방법으로 했습니다"가 최악의 시나리오다.

## Tier 1 — 절대 금지 (어떤 태스크에서도)

1. **DB 스키마 변경** — DDL(CREATE/ALTER/DROP) 실행·작성, 마이그레이션 파일 생성 모두 금지. 스키마는 수동 DDL로 사람이 관리한다. 스키마 변경이 필요하면 태스크에 필요한 DDL 초안만 적고 중단.
2. **운영 데이터 조작** — 운영 DB(Neon)에 대한 UPDATE/DELETE 금지. 특히 계좌 연동 초기 자산 편입의 소급 보정 레코드는 조회조차 신중히.
3. **시크릿·암호화 키** — `APP_ENCRYPTION_KEY` 등 키·토큰·시크릿 값의 생성·수정·출력(로그 포함) 금지. `.env` 파일·환경변수 값을 코드나 커밋에 포함 금지.
4. **배포·CI 설정** — `render.yaml`, Vercel 설정, `.github/workflows/` 수정 금지. CI는 채점자다 — 채점받는 쪽이 채점 기준을 고칠 수 없다.
5. **외부 API 설정** — 한투·ECOS·FRED 등 API 키 설정 변경 금지. 새 외부 API 호출 추가는 사전 승인 필요(무료 티어 사용량 보호).

## Tier 2 — 중단 후 승인 필요

1. **의존성 변경** — 라이브러리 신규 추가, 메이저 버전 업그레이드.
2. **수익률 계산 코어** — TWR/MWR, external flow, FIFO lot 엔진 로직 변경. (테스트가 이 영역을 커버하기 전까지 유지. 커버 후 해제 검토)
3. **인증·인가 로직** — JWT, 세션, 권한 체크 관련 코드.
4. **테스트 삭제·약화** — 실패하는 테스트를 지우거나 assertion을 완화해서 통과시키는 것 금지. 테스트 수정이 정당하면 사유를 PR에 명시하고 승인 대기.

## Tier 3 — 작업 규율

1. **main 직접 push 금지, force push 금지** — 모든 변경은 브랜치 + PR.
2. **머지 금지** — 에이전트의 종점은 PR 생성까지. 머지는 항상 사람.
3. **범위 밖 수정 금지** — 태스크와 무관한 파일의 리네임·포맷팅·리팩토링으로 diff를 오염시키지 않는다. 발견한 개선거리는 태스크에 메모로 남긴다.

## 운영 메모

- 위반이 실제로 발생하면: 사례를 노션 원본 하단에 날짜와 함께 기록 → 규칙 문구를 보강 → 이 파일 갱신.
- Tier 2의 2번(수익률 계산 코어)은 테스트 커버리지 확보(0단계) 완료 후 해제를 검토한다.

## 관련 문서

- 개발 노드 시스템 프롬프트: `.claude/agents/developer.md` (노션 SSOT: AF-165)
